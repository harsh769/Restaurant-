import app from "./app.js";

const PORT = 4000; // Use 'const' to declare the PORT

app.listen(PORT, () => {
  console.log(`SERVER HAS STARTED AT PORT ${PORT}`); // Correct template literal syntax
});
